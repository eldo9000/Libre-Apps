# Stack M1 — Fix Broken Panels

**Phase:** 1 (Foundation & MVP Completion)
**Status:** Complete
**Depends on:** Shared CI setup

---

## Goal

The right-side panel has UI that doesn't work. Search inputs aren't wired to CodeMirror. Format panel selectors are display-only. Fix everything that's built but broken.

---

## Tasks

### Search Panel (Wire to CodeMirror)

- [x] Connect `searchQuery` state to CodeMirror's `@codemirror/search` API
- [x] Find button triggers CM search with current query (not just opens CM's popup)
- [x] Replace and Replace All work through the custom panel UI
- [x] Case-sensitive toggle actually affects search behavior
- [x] Match count displayed (e.g., "3 of 12 matches")
- [x] Enter key in search field triggers find-next
- [x] Escape closes search and returns focus to editor

### Format Panel (Make Functional)

- [x] Line ending selector (LF/CRLF/CR) actually changes the document's line endings on save
- [x] Encoding selector changes the encoding used when reading/writing files
- [x] Tab size selector changes CodeMirror's tab width
- [x] Indent style toggle (tabs vs spaces) affects editor behavior
- [x] Changes persist per-tab (each open file remembers its format settings)

### Code Quality

- [x] Add vitest config to Stack
- [x] Smoke test: app mounts without throwing
- [x] Unit tests: applyLineEnding (6 cases covering all modes + edge cases)
- [ ] Smoke test: open file, edit, save, verify content (deferred — requires deeper Tauri mock)

---

## Definition of Done

- Every UI control in the right panel does what it looks like it does
- Search/Replace works without opening CodeMirror's built-in search popup
- Format changes affect file output
- Basic test suite passes
