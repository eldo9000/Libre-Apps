# Fade M1 — Production Hardening

**Phase:** 1 (Foundation & MVP Completion)
**Status:** Complete
**Depends on:** Shared CI setup

---

## Goal

Fade works for basic conversion but lacks the reliability features needed for daily use. Users need to be able to cancel jobs, see what went wrong, and inspect files before converting.

---

## Tasks

### Cancel / Pause Jobs

- [x] Cancel button on each queued item during conversion
- [x] Cancel sends SIGTERM to the FFmpeg/ImageMagick subprocess
- [x] Cancelled items show "Cancelled" status (not "Error")
- [x] Cancel All button to stop the entire batch
- [x] Partial output files are cleaned up on cancel
- [x] Pause/Resume for batch queue (pause between items, not mid-FFmpeg)

### Error Logging & Reporting

- [x] Capture stderr from FFmpeg/ImageMagick subprocesses (currently discarded via `Stdio::null()`)
- [x] On failure, show the actual error message in the UI (not just "conversion failed")
- [x] Error details expandable in the queue item (click to see full stderr output)
- [x] Log file: write conversion history to `~/.config/librewin/fade.log` (last 100 operations)
- [x] Missing tool detection: on app start, check if `ffmpeg`, `ffprobe`, `magick` are in PATH — show install guidance if missing

### File Info Dialog

- [x] "File Info" button or context menu option for queued files
- [x] Shows: format, codec, resolution, duration, bitrate, file size, audio tracks
- [x] Uses `ffprobe` for video/audio, `magick identify` for images
- [x] Display estimated output size based on current conversion settings (rough estimate)

### Input Validation

- [x] Validate trim_end > trim_start (currently no check)
- [ ] Validate crop dimensions fit within image bounds (TODO: requires get_file_info call before convert — deferred)
- [x] Validate custom resolution format before passing to FFmpeg
- [x] Show validation errors inline (red border + message on the offending field)

### Tests

- [x] Rust: test command building for image/video/audio conversions (verify FFmpeg args are correct)
- [x] Rust: test output path construction with suffix
- [x] Rust: test preset save/load/delete
- [x] Frontend: test queue add/remove/clear
- [x] Frontend: test tab auto-switching on file add
- [x] Integration: test actual image conversion (JPEG → PNG) with sample file (#[ignore], opt-in)

---

## Definition of Done

- Users can cancel any running or queued conversion
- Failed conversions show the actual FFmpeg error message
- File info is inspectable before conversion
- Invalid settings are caught before conversion starts
- Missing external tools are detected and reported on startup
- Test suite covers command building and queue management
