name: CI

on:
  push:
    branches:
      - main
  pull_request:

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  # ── Rust checks ──────────────────────────────────────────────────────────
  rust-check:
    name: Rust check
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Install system dependencies
        run: |
          sudo apt-get update -qq
          sudo apt-get install -y --no-install-recommends \
            build-essential \
            libssl-dev \
            libwebkit2gtk-4.1-dev \
            libgtk-3-dev \
            libayatana-appindicator3-dev \
            librsvg2-dev \
            libglib2.0-dev \
            libsoup-3.0-dev \
            libjavascriptcoregtk-4.1-dev

      - name: Set up Rust
        uses: actions-rust-lang/setup-rust-toolchain@v1
        with:
          cache-workspaces: apps

      - name: cargo check
        working-directory: apps
        run: cargo check --workspace

      - name: cargo clippy
        working-directory: apps
        continue-on-error: true   # workspace lints (unwrap_used etc.) may produce new warnings
        run: cargo clippy --workspace -- -D warnings

      - name: cargo fmt
        working-directory: apps
        run: cargo fmt --check --all

      - name: cargo test
        working-directory: apps
        run: cargo test --workspace

  # ── Linters ──────────────────────────────────────────────────────────────
  # continue-on-error: true on each step so the first sweep lands without
  # forcing a clean pass. Remove continue-on-error once violations are fixed.
  lint:
    name: Lint
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'npm'
          cache-dependency-path: package-lock.json

      - name: Install root devDependencies
        run: npm ci

      - name: ESLint (Svelte 5 + JS)
        continue-on-error: true
        run: npm run lint

      - name: Stylelint (CSS token enforcement)
        continue-on-error: true
        run: npm run lint:css

      - name: Set up Rust
        uses: actions-rust-lang/setup-rust-toolchain@v1
        with:
          cache-workspaces: apps

      - name: cargo fmt --check
        continue-on-error: true
        working-directory: apps
        run: cargo fmt --check --all

      - name: cargo clippy (workspace lints)
        continue-on-error: true
        working-directory: apps
        run: cargo clippy --workspace --all-targets -- -D warnings

  # ── Frontend builds ───────────────────────────────────────────────────────
  frontend-build:
    name: Frontend build (${{ matrix.app }})
    runs-on: ubuntu-latest

    strategy:
      fail-fast: false
      matrix:
        app: [shelf, prism, stack, ghost, fade]

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'npm'
          cache-dependency-path: apps/${{ matrix.app }}/package-lock.json

      - name: Install dependencies
        working-directory: apps/${{ matrix.app }}
        run: npm ci

      - name: Build ${{ matrix.app }}
        working-directory: apps/${{ matrix.app }}
        run: npm run build

      - name: Test ${{ matrix.app }}
        working-directory: apps/${{ matrix.app }}
        run: npm run test
